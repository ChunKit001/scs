spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/test
    username: root

print:
  config: false

logging:
  level:
    root: INFO